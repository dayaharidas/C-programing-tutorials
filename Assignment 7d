#include <stdio.h>

//Compiler version gcc  6.3.0

int main()
{
  int x;
  printf("Enter a number:");
  scanf("%d",&x);
  if (x>0)
  printf("x is positive");
  else if (x<0)
  printf("x is negative");
  else
  printf("0");
  
  return 0;
}
